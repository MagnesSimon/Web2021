import React from "react";

const Connexion = ({element}) => {
    return(
        <header className="header-connexion">
            <h1>Page de connexion</h1>
        </header>
    )
}

export default Connexion