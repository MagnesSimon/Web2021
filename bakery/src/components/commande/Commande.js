import React from "react";

const Commande = ({element}) => {
    return(
        <header className="header-commande">
            <h1>Page de commande</h1>
        </header>
    )
}

export default Commande